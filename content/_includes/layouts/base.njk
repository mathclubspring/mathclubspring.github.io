<!doctype html>
<html lang="ru">
  <head>
    {% set socialTitle = title or "Математический клуб" %}
    {% set socialDescription = summary or "Материалы, программы и календарь математического клуба." %}
    {% set canonicalUrl = "https://mathclubspring.github.io" ~ page.url %}
    {% set socialImage = "https://mathclubspring.github.io/assets/share/default.png" %}
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% if title %}{{ title }} — {% endif %}Математический клуб</title>
    <meta name="description" content="{{ socialDescription | escape }}">
    <link rel="canonical" href="{{ canonicalUrl }}">

    <meta property="og:locale" content="ru_RU">
    <meta property="og:type" content="{% if page.url == "/" %}website{% else %}article{% endif %}">
    <meta property="og:site_name" content="Математический клуб">
    <meta property="og:title" content="{{ socialTitle | escape }}">
    <meta property="og:description" content="{{ socialDescription | escape }}">
    <meta property="og:url" content="{{ canonicalUrl }}">
    <meta property="og:image" content="{{ socialImage }}">
    <meta property="og:image:type" content="image/png">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">
    <meta property="og:image:alt" content="Математический клуб">

    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="{{ socialTitle | escape }}">
    <meta name="twitter:description" content="{{ socialDescription | escape }}">
    <meta name="twitter:image" content="{{ socialImage }}">
    <link rel="stylesheet" href="/assets/site.css">
  </head>
  <body>
    <a class="skip-link" href="#main-content">К основному содержимому</a>
    <header class="site-header">
      <a class="site-name" href="/">Математический клуб</a>
      <nav aria-label="Основная навигация">
        <a href="/"{% if page.url == "/" %} aria-current="page"{% endif %}>Главная</a>
        <a href="/materials/"{% if "/materials/" in page.url %} aria-current="page"{% endif %}>Материалы</a>
        <a href="/events/"{% if "/events/" in page.url %} aria-current="page"{% endif %}>Календарь</a>
        <a href="/olympiads/"{% if "/olympiads/" in page.url %} aria-current="page"{% endif %}>Наши мероприятия</a>
      </nav>
    </header>

    <main id="main-content">
      {{ content | safe }}
    </main>

    <footer>
      <p>Математический клуб</p>
      <nav aria-label="Дополнительная навигация">
        <a href="/contacts/">Контакты</a>
        <a href="/merch/">Наш мерч</a>
        <a href="/privacy/">Privacy Policy</a>
      </nav>
    </footer>
  </body>
</html>
